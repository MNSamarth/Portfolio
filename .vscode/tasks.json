{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Create .gitignore for web project",
			"type": "shell",
			"command": "cmd",
			"args": [
				"/c",
				"echo # Node & web cache > .gitignore && echo node_modules/ >> .gitignore && echo dist/ >> .gitignore && echo build/ >> .gitignore && echo .DS_Store >> .gitignore && echo Thumbs.db >> .gitignore && echo *.log >> .gitignore && echo .env >> .gitignore && echo .venv/ >> .gitignore"
			],
			"problemMatcher": [],
			"group": "build"
		}
	]
}